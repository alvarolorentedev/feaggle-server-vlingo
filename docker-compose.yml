version: '3'
services:
  postgres:
    image: postgres:alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: "feaggle"
      POSTGRES_USER: "feaggle"
      POSTGRES_PASSWORD: "happy-pwd"